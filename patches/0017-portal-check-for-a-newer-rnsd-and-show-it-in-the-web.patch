From 3d5dd73d822e1f9452f2b52273a6662824f7d2f8 Mon Sep 17 00:00:00 2001
From: smit1237 <smit1237@gmail.com>
Date: Wed, 9 Sep 2026 01:14:22 +0300
Subject: [PATCH 17/21] portal: check for a newer rnsd and show it in the web
 UI

Adds a notify-only update checker. rnsbox_portal.updatecheck queries PyPI for
the latest Reticulum "rns" (rnsd) release, compares it to the installed version
(from package metadata, RNS._version fallback), and records the result in
/var/lib/rnsbox/update.json. It never touches the installed package or any user
config, and is offline-safe -- check() never raises and keeps the last known
result.

S84cron runs the checker daily at 05:00 and once, best-effort, ~30s after boot
(gated on /run/clock-synced); the periodic check is toggleable via
/etc/rnsbox/cron.conf (update_check=yes|no). The portal shows the installed rnsd
version plus an "update available" badge on the Reticulum tab and the Dashboard,
with a "Check now" button and an auto-check toggle. store.py's cron.conf helpers
were generalized to whole-dict read/write so update_check and rnsd_restart_days
no longer clobber each other. Upgrading rnsd itself is out of scope (that means
reflashing the image) -- this only notifies.
---
 .../cvitek/SG200X/overlay/etc/init.d/S84cron  |  14 ++
 .../SG200X/overlay/etc/rnsbox/cron.conf       |   8 ++
 .../package/rnsbox-portal/rnsbox-portal.mk    |   2 +
 .../rnsbox-portal/src/bin/rnsbox-update-check |   5 +
 .../rnsbox-portal/src/rnsbox_portal/app.py    |  42 +++++-
 .../rnsbox-portal/src/rnsbox_portal/store.py  |  72 +++++++---
 .../rnsbox_portal/templates/dashboard.html    |   5 +
 .../rnsbox_portal/templates/reticulum.html    |  19 +++
 .../src/rnsbox_portal/updatecheck.py          | 134 ++++++++++++++++++
 9 files changed, 282 insertions(+), 19 deletions(-)
 create mode 100755 buildroot/package/rnsbox-portal/src/bin/rnsbox-update-check
 create mode 100644 buildroot/package/rnsbox-portal/src/rnsbox_portal/updatecheck.py

diff --git a/buildroot/board/cvitek/SG200X/overlay/etc/init.d/S84cron b/buildroot/board/cvitek/SG200X/overlay/etc/init.d/S84cron
index 363f9b609..77fc3db26 100755
--- a/buildroot/board/cvitek/SG200X/overlay/etc/init.d/S84cron
+++ b/buildroot/board/cvitek/SG200X/overlay/etc/init.d/S84cron
@@ -25,6 +25,8 @@ gen_crontab() {
 	mkdir -p "$CRONDIR"
 	days=$(cfg rnsd_restart_days)
 	case "$days" in ''|*[!0-9]*) days=0 ;; esac
+	uc=$(cfg update_check)
+	case "$uc" in no|No|NO|0|false|off) uc=no ;; *) uc=yes ;; esac
 	{
 		echo "# Auto-generated by S84cron from $CONF — do not hand-edit."
 		echo "# (edit /etc/rnsbox/cron.conf or use the portal Reticulum tab)"
@@ -33,10 +35,16 @@ gen_crontab() {
 			# every N days at 04:00
 			echo "0 4 */$days * * /etc/init.d/S82rnsd restart >> $LOG 2>&1"
 		fi
+		if [ "$uc" = yes ]; then
+			# daily at 05:00: notify-only check for a newer rnsd (offline-safe)
+			echo "0 5 * * * /usr/sbin/rnsbox-update-check >> $LOG 2>&1"
+		fi
 	} > "$CRONTAB"
 	chmod 600 "$CRONTAB"
 	[ "$days" -ge 1 ] 2>/dev/null && echo "S84cron: rnsd auto-restart every $days day(s) at 04:00" \
 	                              || echo "S84cron: rnsd auto-restart disabled"
+	[ "$uc" = yes ] && echo "S84cron: daily rnsd update check enabled" \
+	                || echo "S84cron: rnsd update check disabled"
 }
 
 start() {
@@ -46,6 +54,12 @@ start() {
 	if ! pidof crond >/dev/null 2>&1; then
 		crond -b -c "$CRONDIR" 2>>"$LOG"
 	fi
+	# Best-effort update check shortly after boot (once the clock is synced and
+	# WAN may be up). Backgrounded so it never blocks boot; quiet when offline.
+	# ($uc was set by gen_crontab above.)
+	if [ "$uc" = yes ]; then
+		( sleep 30; [ -f /run/clock-synced ] && /usr/sbin/rnsbox-update-check ) >>"$LOG" 2>&1 &
+	fi
 }
 
 stop() {
diff --git a/buildroot/board/cvitek/SG200X/overlay/etc/rnsbox/cron.conf b/buildroot/board/cvitek/SG200X/overlay/etc/rnsbox/cron.conf
index d920c1493..8ab56eb92 100644
--- a/buildroot/board/cvitek/SG200X/overlay/etc/rnsbox/cron.conf
+++ b/buildroot/board/cvitek/SG200X/overlay/etc/rnsbox/cron.conf
@@ -10,3 +10,11 @@
 # starts crond) and live by the portal when you change it in the UI.
 
 rnsd_restart_days=0
+
+# update_check:
+#   yes  (default) once a day, and shortly after boot, check PyPI for a newer
+#        rnsd (Reticulum) release and show a notice in the portal. Notify-only:
+#        it never changes the installed package or any config, and fails
+#        quietly when the box is offline.
+#   no   disable the periodic update check.
+update_check=yes
diff --git a/buildroot/package/rnsbox-portal/rnsbox-portal.mk b/buildroot/package/rnsbox-portal/rnsbox-portal.mk
index 93c9afed8..8d0b6dc69 100644
--- a/buildroot/package/rnsbox-portal/rnsbox-portal.mk
+++ b/buildroot/package/rnsbox-portal/rnsbox-portal.mk
@@ -29,6 +29,8 @@ define RNSBOX_PORTAL_INSTALL_TARGET_CMDS
 		$(TARGET_DIR)/usr/lib/rnsbox-portal/rnsbox_portal/static/
 	$(INSTALL) -D -m 0755 $(@D)/bin/rnsbox-portal \
 		$(TARGET_DIR)/usr/sbin/rnsbox-portal
+	$(INSTALL) -D -m 0755 $(@D)/bin/rnsbox-update-check \
+		$(TARGET_DIR)/usr/sbin/rnsbox-update-check
 endef
 
 $(eval $(generic-package))
diff --git a/buildroot/package/rnsbox-portal/src/bin/rnsbox-update-check b/buildroot/package/rnsbox-portal/src/bin/rnsbox-update-check
new file mode 100755
index 000000000..04fa914b3
--- /dev/null
+++ b/buildroot/package/rnsbox-portal/src/bin/rnsbox-update-check
@@ -0,0 +1,5 @@
+#!/bin/sh
+# Launcher for the rnsd update checker. Queries PyPI for the latest Reticulum
+# 'rns' release and records the result under /var/lib/rnsbox/update.json for the
+# portal to display. Notify-only; offline-safe (exits 0 even with no network).
+exec env PYTHONPATH=/usr/lib/rnsbox-portal python3 -m rnsbox_portal.updatecheck "$@"
diff --git a/buildroot/package/rnsbox-portal/src/rnsbox_portal/app.py b/buildroot/package/rnsbox-portal/src/rnsbox_portal/app.py
index 970d1ca2c..dde2a9559 100644
--- a/buildroot/package/rnsbox-portal/src/rnsbox_portal/app.py
+++ b/buildroot/package/rnsbox-portal/src/rnsbox_portal/app.py
@@ -2,13 +2,14 @@
 import os
 import secrets
 import subprocess
+import time
 from functools import wraps
 
 from flask import (
     Flask, flash, redirect, render_template, request, session, url_for,
 )
 
-from . import auth, store, sysinfo, wifi
+from . import auth, store, sysinfo, updatecheck, wifi
 
 SECRET_FILE = "/run/rnsbox-portal.secret"
 ROUTING_INIT = "/etc/init.d/S60routing"
@@ -91,6 +92,7 @@ def create_app():
             rnsd=sysinfo.rnsd_status(),
             net=sysinfo.network_summary(),
             wan=store.read_wan(),
+            update=updatecheck.display_state(),
         )
 
     # ---- network ----
@@ -230,11 +232,18 @@ def create_app():
         # instantly. The expensive `rnstatus -a` block (a ~5 s cold-start of
         # the RNS stack) is fetched separately by the page via /reticulum/status
         # and dropped in once it arrives — see reticulum.html.
+        upd = updatecheck.display_state()
+        if upd.get("checked_at"):
+            upd["checked_human"] = time.strftime(
+                "%Y-%m-%d %H:%M UTC", time.gmtime(upd["checked_at"]))
         return render_template(
             "reticulum.html",
             rnsd=sysinfo.rnsd_status(),
             cfg=cfg,
             restart_days=store.read_rnsd_restart_days(),
+            rns_version=updatecheck.installed_version(),
+            update=upd,
+            auto_update=store.read_update_check(),
         )
 
     @app.route("/reticulum/autorestart", methods=["POST"])
@@ -256,6 +265,37 @@ def create_app():
             flash(f"Error: {e}", "error")
         return redirect(url_for("reticulum"))
 
+    @app.route("/reticulum/checkupdate", methods=["POST"])
+    @login_required
+    def reticulum_checkupdate():
+        # Notify-only: queries PyPI, records state; never touches the install
+        # or config. Synchronous but short (a few seconds), offline-safe.
+        st = updatecheck.check()
+        if st.get("error"):
+            flash("Update check failed — the box may be offline. Last known "
+                  "latest: " + (st.get("latest") or "unknown") + ".", "error")
+        elif st.get("update_available"):
+            flash("Update available: rns %s (installed %s). To upgrade, build "
+                  "and flash the latest RNSBox image." %
+                  (st.get("latest"), st.get("installed")), "success")
+        else:
+            flash("rnsd is up to date (%s)." % (st.get("installed") or "unknown"),
+                  "success")
+        return redirect(url_for("reticulum"))
+
+    @app.route("/reticulum/autoupdate", methods=["POST"])
+    @login_required
+    def reticulum_autoupdate():
+        enabled = request.form.get("auto_update") == "on"
+        try:
+            store.write_update_check(enabled)
+            subprocess.run(["/etc/init.d/S84cron", "reload"], check=False, timeout=10)
+            flash("Automatic update checks " +
+                  ("enabled (daily)." if enabled else "disabled."), "success")
+        except Exception as e:
+            flash(f"Error: {e}", "error")
+        return redirect(url_for("reticulum"))
+
     @app.route("/reticulum/status")
     @login_required
     def reticulum_status():
diff --git a/buildroot/package/rnsbox-portal/src/rnsbox_portal/store.py b/buildroot/package/rnsbox-portal/src/rnsbox_portal/store.py
index e35f1024b..483b2dad8 100644
--- a/buildroot/package/rnsbox-portal/src/rnsbox_portal/store.py
+++ b/buildroot/package/rnsbox-portal/src/rnsbox_portal/store.py
@@ -233,33 +233,69 @@ def write_wan(iface):
     write_wan_config(cfg)
 
 
-# ---- scheduled tasks (rnsd auto-restart cron) ----
+# ---- scheduled tasks (cron.conf: rnsd auto-restart + update check) ----
+#
+# cron.conf holds multiple flat key=value settings; read/write go through the
+# whole-dict helpers so setting one key never clobbers the others.
 
-def read_rnsd_restart_days():
-    """Return the rnsd auto-restart interval in days (0 = disabled)."""
+def read_cron_conf():
+    """cron.conf as a {key: value} dict."""
+    d = {}
     for ln in _read_lines(CRON_FILE):
         s = ln.strip()
         if s.startswith("#") or "=" not in s:
             continue
         k, _, v = s.partition("=")
-        if k.strip() == "rnsd_restart_days":
-            v = v.split("#", 1)[0].strip()
-            try:
-                n = int(v)
-            except ValueError:
-                return 0
-            return n if 0 <= n <= 7 else 0
-    return 0
+        d[k.strip()] = v.split("#", 1)[0].strip()
+    return d
 
 
-def write_rnsd_restart_days(days):
-    """Persist the rnsd auto-restart interval (0 = off, 1..7 days)."""
-    days = int(days)
-    if not (0 <= days <= 7):
-        raise ValueError("rnsd_restart_days must be 0..7")
+def _truthy(v):
+    return str(v).strip().lower() not in ("no", "0", "false", "off", "")
+
+
+def write_cron_conf(d):
+    """Rewrite cron.conf from a dict, preserving both known keys."""
+    try:
+        days = int(d.get("rnsd_restart_days", 0) or 0)
+    except (TypeError, ValueError):
+        days = 0
+    days = days if 0 <= days <= 7 else 0
+    uc = _truthy(d.get("update_check", "yes"))
     _write_lines(CRON_FILE, [
         "# RNSBox scheduled tasks — managed by the rnsbox-portal Reticulum tab.",
-        "# rnsd_restart_days: 0 = disabled (default); N = restart rnsd every N",
-        "# days at 04:00 (memory hygiene). Applied by S84cron + live by the UI.",
+        "# rnsd_restart_days: 0 = off (default); N = restart rnsd every N days at",
+        "#   04:00 (memory hygiene). update_check: yes (default) / no — daily",
+        "#   check for a newer rnsd (notify-only). Applied by S84cron; live via UI.",
         f"rnsd_restart_days={days}",
+        f"update_check={'yes' if uc else 'no'}",
     ])
+
+
+def read_rnsd_restart_days():
+    """rnsd auto-restart interval in days (0 = disabled)."""
+    try:
+        n = int(read_cron_conf().get("rnsd_restart_days", "0"))
+    except ValueError:
+        return 0
+    return n if 0 <= n <= 7 else 0
+
+
+def write_rnsd_restart_days(days):
+    days = int(days)
+    if not (0 <= days <= 7):
+        raise ValueError("rnsd_restart_days must be 0..7")
+    d = read_cron_conf()
+    d["rnsd_restart_days"] = days
+    write_cron_conf(d)
+
+
+def read_update_check():
+    """Whether the daily rnsd update check is enabled (default True)."""
+    return _truthy(read_cron_conf().get("update_check", "yes"))
+
+
+def write_update_check(enabled):
+    d = read_cron_conf()
+    d["update_check"] = "yes" if enabled else "no"
+    write_cron_conf(d)
diff --git a/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/dashboard.html b/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/dashboard.html
index ec61a7019..795f3bf80 100644
--- a/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/dashboard.html
+++ b/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/dashboard.html
@@ -68,6 +68,11 @@
       {% if rnsd.running and rnsd.rss_mb %}
       <tr><th>rnsd RAM</th><td>{{ rnsd.rss_mb }} MB RSS</td></tr>
       {% endif %}
+      <tr><th>Version</th><td>
+        Reticulum (rnsd) <strong>{{ update.installed or 'unknown' }}</strong>
+        {% if update.update_available %}<span class="badge warn">update available: {{ update.latest }}</span>
+        {% elif update.latest and not update.error %}<span class="badge ok">up to date</span>{% endif %}
+      </td></tr>
     </table>
     <p><a class="btn" href="{{ url_for('reticulum') }}">Manage →</a></p>
   </section>
diff --git a/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/reticulum.html b/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/reticulum.html
index 5e32f8a16..dc51858d0 100644
--- a/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/reticulum.html
+++ b/buildroot/package/rnsbox-portal/src/rnsbox_portal/templates/reticulum.html
@@ -9,11 +9,30 @@
     <tr><th>State</th><td id="rnsd-state">
       {% if rnsd.running %}<span class="badge ok">RUNNING</span> (pid {{ rnsd.pid }}){% else %}<span class="badge err">STOPPED</span>{% endif %}
     </td></tr>
+    <tr><th>Version</th><td>
+      Reticulum (rnsd) <strong>{{ rns_version or 'unknown' }}</strong>
+      {% if update and update.update_available %}<span class="badge warn">update available: {{ update.latest }}</span>
+      {% elif update and update.latest and not update.error %}<span class="badge ok">up to date</span>{% endif %}
+    </td></tr>
+    <tr><th>Update check</th><td>
+      {% if update and update.checked_human %}last checked {{ update.checked_human }}{% else %}not checked yet{% endif %}
+      {% if update and update.error %} <span class="muted">— last attempt failed (offline?)</span>{% endif %}
+    </td></tr>
   </table>
   <form method="post" action="{{ url_for('reticulum_restart') }}" class="inline">
     <button type="submit" class="btn-primary">Restart rnsd</button>
   </form>
   <button type="button" class="btn" onclick="loadRnstatus()">Refresh</button>
+  <form method="post" action="{{ url_for('reticulum_checkupdate') }}" class="inline">
+    <button type="submit" class="btn">Check for updates</button>
+  </form>
+
+  <form method="post" action="{{ url_for('reticulum_autoupdate') }}" class="row-form" style="margin-top:10px">
+    <label style="margin:0"><input type="checkbox" name="auto_update"
+      {{ 'checked' if auto_update else '' }} onchange="this.form.submit()"> check for updates daily</label>
+    <noscript><button type="submit" class="btn">Save</button></noscript>
+    <span class="muted">notify-only — a newer rnsd means reflashing the latest RNSBox image; nothing here changes your config</span>
+  </form>
 
   <form method="post" action="{{ url_for('reticulum_autorestart') }}" class="row-form" style="margin-top:10px">
     <label for="rnsd_days" style="margin:0">Auto-restart rnsd</label>
diff --git a/buildroot/package/rnsbox-portal/src/rnsbox_portal/updatecheck.py b/buildroot/package/rnsbox-portal/src/rnsbox_portal/updatecheck.py
new file mode 100644
index 000000000..cab7d4c71
--- /dev/null
+++ b/buildroot/package/rnsbox-portal/src/rnsbox_portal/updatecheck.py
@@ -0,0 +1,134 @@
+"""Check whether a newer rnsd (the Reticulum 'rns' package) is available.
+
+Runs periodically from cron (and once, best-effort, at boot); it queries PyPI
+for the latest 'rns' release, compares it to the installed version, and writes
+a small JSON state file the portal reads to show an "update available" notice.
+
+Deliberately a *notifier only*:
+  * it never touches the installed package or any user config -- the only file
+    it writes is the machine-state JSON under /var/lib/rnsbox (never /etc);
+  * network failures are non-fatal (the box may be offline) -- check() never
+    raises and preserves the last known result.
+"""
+import json
+import os
+import time
+import urllib.request
+
+PKG = "rns"                       # rnsd is provided by the Reticulum 'rns' pkg
+STATE_DIR = "/var/lib/rnsbox"
+STATE_FILE = STATE_DIR + "/update.json"
+PYPI_URL = "https://pypi.org/pypi/{pkg}/json"
+TIMEOUT = 8
+
+
+def installed_version(pkg=PKG):
+    """Installed version string, or None. Cheap: reads package metadata
+    (no heavy `import RNS`), with RNS._version as a fallback."""
+    try:
+        import importlib.metadata as md
+        return md.version(pkg)
+    except Exception:
+        pass
+    try:                          # fallback: RNS ships _version.py
+        import RNS
+        return getattr(RNS, "__version__", None)
+    except Exception:
+        return None
+
+
+def _vtuple(v):
+    """Leading numeric components of a version as an int tuple ('1.5.2' ->
+    (1,5,2)); trailing non-numeric (rc/beta) parts are ignored per component."""
+    out = []
+    for part in str(v).split("."):
+        num = ""
+        for ch in part:
+            if ch.isdigit():
+                num += ch
+            else:
+                break
+        if num == "":
+            break
+        out.append(int(num))
+    return tuple(out)
+
+
+def _newer(latest, installed):
+    if not latest or not installed:
+        return False
+    return _vtuple(latest) > _vtuple(installed)
+
+
+def latest_version(pkg=PKG, timeout=TIMEOUT):
+    req = urllib.request.Request(
+        PYPI_URL.format(pkg=pkg),
+        headers={"User-Agent": "rnsbox-update-check",
+                 "Accept": "application/json"})
+    with urllib.request.urlopen(req, timeout=timeout) as r:
+        data = json.load(r)
+    return data["info"]["version"]
+
+
+def read_state():
+    """Last recorded state dict (empty if never checked / unreadable)."""
+    try:
+        with open(STATE_FILE) as f:
+            return json.load(f)
+    except Exception:
+        return {}
+
+
+def display_state(pkg=PKG):
+    """State for the web UI. Keeps the last check's `latest`/`checked_at`/
+    `error`, but recomputes `installed` and `update_available` against the
+    version installed RIGHT NOW. Otherwise an update applied out of band —
+    a manual `pip install -U rns`, or apply_update itself — still shows a
+    stale 'update available' until the next periodic check rewrites the file."""
+    state = read_state()
+    installed = installed_version(pkg)
+    state["installed"] = installed
+    latest = state.get("latest")
+    state["update_available"] = bool(latest) and _newer(latest, installed)
+    return state
+
+
+def _write_state(state):
+    try:
+        os.makedirs(STATE_DIR, exist_ok=True)
+        tmp = STATE_FILE + ".tmp"
+        with open(tmp, "w") as f:
+            json.dump(state, f)
+        os.rename(tmp, STATE_FILE)
+    except Exception:
+        pass                       # best-effort; never fatal
+
+
+def check(pkg=PKG):
+    """Query PyPI, compare, persist. Never raises; returns the state dict."""
+    state = read_state()           # keep prior 'latest' if this attempt fails
+    state.update({"package": pkg,
+                  "installed": installed_version(pkg),
+                  "checked_at": int(time.time())})
+    try:
+        latest = latest_version(pkg)
+        state["latest"] = latest
+        state["update_available"] = _newer(latest, state["installed"])
+        state["error"] = ""
+    except Exception as e:
+        state["error"] = type(e).__name__
+    _write_state(state)
+    return state
+
+
+if __name__ == "__main__":
+    st = check()
+    if st.get("error"):
+        print("update-check: failed (%s); installed=%s"
+              % (st["error"], st.get("installed")))
+    elif st.get("update_available"):
+        print("update-check: %s %s available (installed %s)"
+              % (st.get("package"), st.get("latest"), st.get("installed")))
+    else:
+        print("update-check: %s up to date (%s)"
+              % (st.get("package"), st.get("installed")))
-- 
2.47.3

